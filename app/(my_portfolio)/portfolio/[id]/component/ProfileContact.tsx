import React from 'react';

interface ProfileContactProps {
    user: any
}
const ProfileContact: React.FC<ProfileContactProps> = ({ user }) => {
    return (
        <div>
            contact,,,,,,,,,
        </div>
    );
};

export default ProfileContact;